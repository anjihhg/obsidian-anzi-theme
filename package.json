{
	"name": "sample-obsidian-theme",
	"version": "1.0.0",
	"scripts": {
		"build": "esbuild src/theme.css --bundle --outfile=theme.css",
		"dev": "node esbuild.config.mjs",
		"version": "node version-bump.mjs && git add manifest.json versions.json"
	},
	"devDependencies": {
		"esbuild": "^0.27.2"
	}
}
